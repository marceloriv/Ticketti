import clienteApi from '../api/clienteApi';

/**
 * Re-exportación para mantener compatibilidad con imports existentes en la aplicación
 * que referencian a @services/api.
 */
export default clienteApi;
